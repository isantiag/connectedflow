{
  "name": "@connectedflow/integration-services",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/",
    "test": "vitest --run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@connectedflow/shared-types": "*",
    "@connectedflow/core-services": "*"
  },
  "devDependencies": {
    "vitest": "^4.1.2"
  }
}
